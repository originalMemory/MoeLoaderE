## 1. 源基线与资源

- [x] 1.1 记录相关源文件 SHA-256、HEAD 和未提交修改，完整读取本阶段调用路径。
- [x] 1.2 使用独立测试配置运行源应用，取得初始、搜索结果、参数、菜单和预览的对照截图；记录窗口尺寸、缩放和主题。
- [x] 1.3 建立源功能逐项清单，复用资源或转写代码时加入 GPLv3 许可证与来源说明。

## 2. 主窗口与搜索界面

- [x] 2.1 按源资源和 XAML 实现主窗口、初始搜索布局及搜索后的布局切换。
- [x] 2.2 转写站点选择、关键词输入、提示和本阶段支持的参数控件，核对默认值、状态和文案。
- [x] 2.3 转写状态栏、分页控件、图片尺寸滑块，保留后续功能入口并明确未完成状态。

## 3. 单站点与进程通信

- [x] 3.1 核对当前 Electron API，建立受限 preload 方法与 IPC 参数、调用方验证。
- [x] 3.2 转写 Konachan-G 的查询、提示、评级和字段映射；保留源默认行为与 Referer。
- [x] 3.3 转写搜索会话、取消、过滤和分页，阻止过期响应覆盖当前结果。
- [x] 3.4 接入受限图片获取，核对地址、重定向、响应类型及资源释放，不暴露通用代理。

## 4. 图片列表与预览

- [x] 4.1 转写图片条目、加载反馈、已读状态及计数，按源布局排列并响应尺寸变化。
- [x] 4.2 转写单选、全选、Shift 范围选择、框选、右键菜单及 Ctrl+R 重试语义。
- [x] 4.3 转写独立预览窗口、图片选择、元信息与标签展示。
- [x] 4.4 转写预览尺寸、滚轮缩放、拖动限制、Escape 与加载失败处理。

## 5. 验证与阶段 review

- [x] 5.1 用固定样本对照查询、字段映射、过滤、分页和已读语义；覆盖特殊字符、空结果、缺失字段、取消与失败。
- [x] 5.2 更新桌面检查，覆盖界面状态、选择、预览与非法 IPC；保留启动隔离和失败退出码回归检查。
- [x] 5.3 执行 Windows 开发、生产构建和桌面验证；源画面与新画面在相同条件下逐项对照。
- [x] 5.4 单独记录 Konachan-G 在线搜索及图片加载结果；macOS 未实测时不勾选平台验收。
- [x] 5.5 汇总已转写、未完成、未验证和需确认差异，更新路线图，停止等待用户 review。

5.3 已完成：Windows 10 材质、主题、控件与布局对照通过；量测及截图见 review.md。macOS 不计入本机验收结果。

## 6. 整体 review 修复

- [x] 6.1 只记录未读图片；兼容已有负偏移记录并验证重载、启动及非法区间拒绝。
- [x] 6.2 下拉菜单使用顶层弹出层并按可用空间翻转；验证 760/400 高度下的鼠标选择与焦点切换。
- [x] 6.3 为每条结果分配独立键，贯通加载、选择、预览、进度与导出；验证重复业务 ID 不互相覆盖。
