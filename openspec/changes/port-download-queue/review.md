# 阶段 3 review

## 范围与来源

- 2026-09-16 用户授权继续下载主体；参考本机 MoeLoaderP f37c511。7 个 Windows 工作区优化保留在阶段 2 差异记录，不作为当前阻塞。
- 对照读取 MoeDownloader、MoeItem、MoeItemHelper、Settings、DownloaderControl XAML/code-behind、MoeResource 下载模板、UnfinishedDownloadMlpub 和主窗口关闭逻辑。
- 默认最多 3 个父任务并发；组图内部顺序执行。默认目录为系统图片目录/MoeLoaderE，默认同名跳过，文件/目录模板沿用原版。

## 已实现

- 单张/所选/右键/快捷键入队；所选入队后清空选择并打开 220px 侧栏。
- 原图、预览图、自动优先原图；入队时保存来源及设置快照，不依赖当前搜索继续存在。
- 42px 任务行、缩略图、进度、状态文字、选择/范围选择/全选、打开文件位置、停止、删除、重试、全部开始/停止、清理完成。
- 下载专用设置：目录、并发、文件/分类模板、标签数量、同名自动改名、组图前 N 张；独立 downloads.json。
- 500 秒请求超时，最多 3 次尝试，复用站点 Session、Referer 和地址/重定向白名单。流式写文件，无预览的 40 MiB 缓冲限制。
- 随机独占 .part 临时文件、完整性检查、独占发布最终文件，失败/停止/删除清理临时文件。同名竞争不覆盖，自动改名使用 -2、-3 等后缀。
- .mlpub v1 导入（拖入下载面板）、未完成任务导出与合并去重；关闭主窗口或退出提供导出/直接关闭/取消，保存取消或失败保留窗口。

## 与源程序的明确差异

- 7 个未同步的优化不在此次还原范围；未更改旧 source-hashes.json。
- 源直接写最终文件改为先写临时文件再独占提交；停止/重试换用新取消信号，等待旧流清理后才复用并发槽。
- 源重试未覆盖 Stop、取消覆盖状态、组图失败统计和自动改名路径错误未照搬；停止任务可重试，组图有失败不报整体成功。
- 模板按完整 token 替换，兼容 %upid / %uploader_id，避免 %site 提前替换 %sitedispname；分类分隔符跨平台转换。非法字符/路径、保留文件名和 UTF-8 文件名长度做边界处理。
- 原 .mlpub 导出跳过组图；这里导出未完成子项，已完成子项不会重新导出。导入保留安全处理后的名称；仅接受当前已迁移站点和明确 HTTPS 域名，拒绝任意网络代理或本地路径。
- 原右键全部开始/停止有 UI 但未绑定，这里接通已有队列动作。
- 已撤销自创侧栏工具栏和独立设置弹窗。恢复顶部“软件设置”滚动浮层、原顺序分组，下载参数即时生效；非本阶段设置保留原入口/外观，操作时明确提示尚未迁移并恢复值，不实现后续阶段逻辑。
- Mac 关闭主窗口并确认后停止任务，进程仍遵循 Mac 生命周期；再激活可查看并重试队列。退出进程后通过 .mlpub 恢复，不暗中自动恢复队列。

## 验证与边界

- 文件测试：实际读取字节、并发上限、同名跳过/竞争改名、断流三次失败、停止/删除清理、重试、组图失败与前 N 张、任务包合并、非法 URL/模板路径/符号链接越界。
- 桌面测试：单张原图、所选预览和自动类型实际落盘；切换搜索仍完成旧任务；下载 IPC 校验；设置持久化；任务包导入导出；关闭取消和保存取消；保留原 6 项启动/安全/浏览/视觉回归。
- 真实组图站点不在当前范围：静态组图引擎经过可控流与真实文件验证；其他站点的动态分页解析和下载后处理随站点阶段迁移。
- 本机为 macOS arm64；Windows 下载及安装包未实测，未声称全平台发布验收完成。

## 本轮命令证据

- `npm test`：类型检查、生产构建及 15/15 测试通过，无跳过。
- `npm run test:online`：10 张搜索结果/缩略图、独立预览和真实原图下载通过，落盘 1,786,700 字节；核对文件长度及图片签名，测试后清理临时 userData 和下载文件。
- 在线检查曾出现一次搜索返回空页，最终重试成功；此前阶段也记录过偶发分页现象，不声称已定位或修复。在线测试改为数量输入 Tab 提交并保留失败请求 URL。
- 修正了新测试中异步 waitForFunction 提前结束等待的问题，改为等待 DOM 终态再读实际文件；所有落盘断言在最终完整回归中重新通过。
- 截图已检查：`artifacts/download-results.png`、`download-settings.png`、`download-online.png`。侧栏宽度和任务行按源 XAML，未在 Mac 上运行 WPF 做像素级对比。

## 下载 UI 对齐复核

- 依据当前参考 XAML 与 code-behind 重写 UI；不再把上一版自行设计的工具栏和模态设置弹窗标记为等价实现。
- 下载侧栏：220px，透明底色；移除顶部工具栏、“无任务”文字和文件选择入口，使用源拖入导入。列表内边距 3px，单图行 42px、行间距 3px，38px 缩略图框及 42/22/剩余宽度的列布局。
- 进度：源灰白轨道、蓝色渐变指示、0.6 不透明度和圆角；选中描边 #CA007CFF、白色悬浮遮罩、0.5 秒入场动画。组图子行 20px，原路径箭头、编号、状态图标和进度。
- 菜单：158px 内容宽度、4px 内边距、22px 菜单行；恢复源图标、ALL 标记、顺序和“全选(Ctrl+A)”文案。Mac 同时支持 Command，但保留源文案。
- 设置：顶部同名开关，310×560 内容视口、6px 边框内边距、滚动浮层，原分组顺序、下载项顺序和文案，无保存/取消按钮。模板聚焦显示 13 个插入按钮，失焦隐藏及清理非法字符；恢复默认、目录浏览和前 N 张启用状态按源处理。
- 下载 UI 输入范围按源设置为并发 1–15、前 N 张 1–99、命名标签数量 0–100。未搜索时打开下载栏，居中搜索区域随左列缩小；搜索后仍按源跨列置于右上。
- `tests/fixtures/wpf-download-layout.json` 保存源声明值，`download-ui.test.mjs` 验证布局、全部状态样本、菜单、命名插入/恢复默认、即时保存、外部点击关闭、小窗口不裁切；原桌面闭环改为真实拖入任务包。
- 已检查初始设置、滚动后下载设置、命名按钮、空列表、任务各状态、组图及菜单截图。证据为 `artifacts/download-ui-*.png` 与 `download-ui-metrics.json`。
- 验收边界：本机不能运行 WPF，以上是源码声明/交互路径对照和 Electron 实测，不是 Windows 原生截图逐像素验收。字体沿用已接受的 Mac 回退字体；小窗口浮层受 Electron 内容区限制缩小滚动视口。没有宣称整应用未迁移功能已完成。

## 整体 review 问题修复

- 命名：移除 token 后的单词边界限制，仍按长 token 优先匹配；验证 `%id_%uploader` 两个作品分别落盘、不误跳过，包含相邻 token、后缀和 `%uploader_id`。
- 取消竞态：目录和文件存在性检查完成后、任何状态写入之前再次检查取消信号；验证入队后立即停止仍为 stopped，立即重试仅发出一次新请求并成功落盘。
- 任务包：导出及合并复用回读校验（10000 条、8 MiB 和字段格式）。合并使用副本；超限明确报错，不写文件、不关闭窗口。不自动拆包，保留当前任务供继续下载或调整后重试。
- 列表：普通 Shift 替换范围外选择，Ctrl/Command+Shift 追加范围；连续 Shift 保持锚点。锚点和焦点按任务 ID 跟随队列更新。补齐上下方向键、Home/End、PageUp/PageDown、空格，以及修饰键选择/仅移动焦点；使用单一 Tab 入口。
- 新增用例先在旧实现复现 4 项失败，再修复并通过完整回归；桌面测试确认超限“导出后关闭”不会退出，也不会覆盖已有任务包。

## Mac 材质补验

用户继续授权后，已完成同屏材质/遮罩 A/B 与真实窗口整屏验证：保留 under-window，仅减轻 Mac 活动态页面染色；原毛玻璃开关接通、主预览同步及持久化，系统降低透明度/失焦仍回退。详见 `mac-acrylic.md`。非本阶段设置中，毛玻璃已不再是占位项；其他入口仍按阶段边界处理。
