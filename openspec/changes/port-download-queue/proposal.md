# 阶段 3：下载主体迁移

用户于 2026-09-16 授权继续主体逻辑。以本机 `../MoeLoaderP` f37c511 为参考；7 个 Windows 未提交优化已记录且经用户确认不阻塞。

范围：Konachan-G 单张/所选入队、原图/预览/自动类型，默认 3 并发，进度、停止/删除/重试/清理完成，命名/分类/同名策略，静态组图顺序下载，临时文件安全落盘，.mlpub 导入导出及退出保护。保留原版 220px 下载侧栏、42px 任务行、右键菜单。只提供本阶段必要的下载设置，不实现账号、代理和其他站点。

本阶段跨主进程、IPC、渲染 UI，使用现有 OpenSpec 文档结构，不使用 speclite，也不进入后续登录阶段。
