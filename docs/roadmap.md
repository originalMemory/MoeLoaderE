# MoeLoaderE 路线图

## 目标与决策

- 将 MoeLoaderP 换语言等价转写：逻辑、UI 布局、样式、文案和交互保持一致，不重新设计产品。
- 已确定：Electron + TypeScript；不保留 C# 运行时。
- 目标平台：Windows、macOS 桌面端；Linux 暂不考虑。移动端与 Web 版另行评估。
- 主进程负责站点请求、会话和文件操作；渲染进程负责界面。实际需要通信时才添加明确、受限的 preload API。
- 登录页使用隔离的浏览器上下文，不开放 Node 权限；站点请求与下载需统一 Cookie、代理和 Referer。
- 初版使用本地 JSON 保存设置；不预建数据库、插件框架、通用任务系统。
- 站点逐个验证；旧版存在实现不等于接口仍可用。

## 等价迁移约束

- 2026-09-15 用户明确要求逻辑与 UI 保持源项目一致；此要求优先于此前关于重做界面的表述。
- 当前基线：本机 `../MoeLoaderP` 提交 `f1fd37c`。此前记录的 7 个 Windows 工作区差异文件及对应规格已在该提交同步。
- 2026-09-20 已复核 `f37c511..f1fd37c`：Electron 原有实现已覆盖 Shift 区间选择、组图失败汇总、失败重试信号和搜索异常完成状态；本轮补齐 240 字节文件名限制、系统任务栏/Dock 下载进度及“清除成功并重试失败”。
- 2026-09-16 用户再次确认 UI 必须等价迁移；不得自创新工具栏、弹窗或保存流程。Mac 字体差异已接受；不能以功能通过替代 UI 验收。
- 每个功能开始前读取对应源文件，追踪入口、状态和异常路径；源工作区若变化，在当前 spec 记录基线变动。
- UI 对照源窗口运行画面、XAML、资源与事件处理：布局、颜色、字体、图标、菜单、快捷键、框选、分页、预览、设置和状态反馈逐项验收。
- 逻辑使用相同输入或本地样本对照输出：站点参数、解析、排序过滤、命名、重复文件处理、队列状态与重试、登录和代理。失效外部站点单独记录，不以联网失败替代逻辑验收。
- 每个阶段 review 附本阶段源功能清单、已转写项、截图或行为验证结果、未完成项及差异。
- 平台不支持的效果或系统行为、源代码缺陷及确需调整的行为单独列为差异，在 review 中确认；不默默删减或改成另一种交互。
- 阶段 1 占位页已被阶段 2 浏览界面替换；Windows 视觉补齐已通过量测与状态对照，整体 review 问题已修复。

## 阶段进度

| 阶段 | 范围 | 状态 | 验收结果 |
| --- | --- | --- | --- |
| 0 | Git、路线图、首阶段 spec | 已完成 | 文档已建立 |
| 1 | Electron 工程、最小窗口、开发与构建命令 | 已完成，review 问题已修复 | [spec](../specs/speclite/electron-bootstrap/spec.md)；Windows 运行及启动失败退出码验证通过，macOS 未实测 |
| 2 | 对照源主界面、搜索与预览 UI，接通单个 Booru 站点 | 已完成，review 问题已修复 | [验收记录](../openspec/changes/port-booru-browser/review.md)；24/24，6 项测试及在线验证通过 |
| 3 | 下载队列、取消重试、命名、组图与临时文件 | 已完成，review 修复已验收 | [验收记录](../openspec/changes/port-download-queue/review.md)；Mac 15 项测试与真实下载通过；整体 review 的 4 项问题已修复；下载 UI 已按源 XAML 对齐，组图使用引擎/界面样本验证 |
| 4 | 登录、Cookie、代理与需登录站点 | 已实现，review 问题已修复，真实账号验收留待 Windows | [验收记录](../openspec/changes/port-session-auth/review.md)；21 项测试通过，真实登录页/系统代理已验证，账号操作尚未验证 |
| 5 | 其他站点、自定义站点、设置与历史记录 | 进行中 | 搜索设置/历史、背景/低性能、自定义站点、Safebooru、Yande 已验收；[Gelbooru](../openspec/changes/port-gelbooru/review.md) 样本通过，匿名实站确认 DAPI 需认证；其余站点待迁移（第三级详情仅源字段，未发现执行逻辑） |
| 6 | Windows/macOS 打包与发布验证 | 未开始 | 未验证 |

登录技术验证在批量扩展站点前完成；若它影响网络结构，可提前到阶段 2 后执行。

- 2026-09-16 用户授权提交推送阶段 4 并继续阶段 5；Pixiv 真实账号登录、重启 Cookie 与下载验收留到 Windows，不阻塞后续主体迁移。

## 跟踪规则

- 测试按实际影响范围选择；功能提交或新增站点不默认跑全量。优先验证新站点及受影响的共享链路，仅影响范围广、难隔离或发布验收时考虑全量。

- 本文件记录总体进度；当前功能的 spec 记录实施任务、关键决策和验收证据。
- 小范围改动用 speclite；跨 3 个以上独立模块的变更按 speclite 约定转 OpenSpec，不将整个项目塞进一个 spec。
- 每次只细化当前阶段；阶段不是必须由一份 spec 完成的任务。
- spec 确认后再实现；开始或恢复实现前重新读取最新 spec。
- 每阶段完成并验证后停止，提交阶段结果供用户 review；用户确认继续后才进入下一阶段。
- 已实现、已验证分别记录；验证失败或缺少目标平台环境时保持未完成。
- 不在进度文档记录账号、Cookie、令牌或其他登录凭据。

## 尚未确定

- 第二阶段沿用 HTML/CSS/TypeScript，按源布局和交互转写；Windows 视觉补齐及 review 修复完成。
- 第二阶段 Konachan-G 在线搜索、10 张缩略图与独立预览已验证；需登录站点在阶段 4 确定。
- 最低操作系统版本、发布架构、安装包格式：在发布阶段明确。
- 原版设置与自定义站点 JSON：按原有语义迁移；跨平台路径等差异逐项列出供 review。

## 平台验收

| 平台 | 开发运行 | 打包运行 | 登录与代理 | 搜索与下载 |
| --- | --- | --- | --- | --- |
| Windows | 已验证（阶段 2） | 未验证（生产构建运行已通过，尚未生成安装包） | 未验证 | 搜索与预览已验证；阶段 3 下载未在 Windows 实测 |
| macOS | 已验证（arm64，阶段 4；含 Command 快捷键） | 未验证 | 代理及登录样本已验证；真实账号待验 | Konachan-G 在线通过；Pixiv 静态流程样本通过 |

- Mac 毛玻璃对照优化：保留系统材质、减少重复染色，开关和系统透明度限制已接通；见 [检查与实现记录](../openspec/changes/port-download-queue/mac-acrylic.md)。
